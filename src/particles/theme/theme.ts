import { createTheme } from "@shopify/restyle";
// import tokens from "./tokens";

export const neutralTheme = createTheme({
  breakpoints: {},
  colors: {},
  spacing: {},
});

export const lightTheme = Object.assign(neutralTheme, {});
